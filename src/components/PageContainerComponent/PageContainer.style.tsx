import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  safe: { height: '100%', backgroundColor: 'white' },
  view: {
    height: '100%',
    paddingHorizontal: 24,
    paddingTop: 62,
  },
});
